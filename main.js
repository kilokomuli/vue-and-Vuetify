// creating a vue app
// {} option object
const app = Vue.createApp ({
    // as an argument we pass object and add a data property 
    data() {
        return {
            product : 'Socks',
            description : 'This are my socks'
        }
    },
});
